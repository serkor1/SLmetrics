% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{weighted.precision.factor}
\alias{weighted.precision.factor}
\title{Precision}
\usage{
\method{weighted.precision}{factor}(actual, predicted, w, estimator = 0L, na.rm = TRUE, ...)
}
\arguments{
\item{actual, predicted}{A pair of vectors <\link{factor}> with actual and observed classes.}

\item{w}{A <\link{numeric}> vector of sample weights.}

\item{estimator}{An <\link{integer}>-value of \link{length} \eqn{1} (default: \eqn{0}).
\itemize{
\item 0 - a named <\link{numeric}>-vector of \link{length} k (class-wise)
\item 1 - a <\link{numeric}> value (Micro averaged metric)
\item 2 - a <\link{numeric}> value (Macro averaged metric)
}}

\item{na.rm}{A <\link{logical}> value of \link{length} \eqn{1} (default: \link{TRUE}). If \link{TRUE}, \link{NA} values are removed from the computation.
This argument is only relevant when \code{micro != NULL}.
When \code{na.rm = TRUE}, the computation corresponds to \code{sum(c(1, 2, NA), na.rm = TRUE) / length(na.omit(c(1, 2, NA)))}.
When \code{na.rm = FALSE}, the computation corresponds to \code{sum(c(1, 2, NA), na.rm = TRUE) / length(c(1, 2, NA))}.}

\item{...}{Arguments passed into other methods.}
}
\description{
A generic funcion for the \href{https://en.wikipedia.org/wiki/Positive_and_negative_predictive_values}{precision}. Use \code{\link[=weighted.fdr]{weighted.fdr()}} for the weighted \href{https://en.wikipedia.org/wiki/Positive_and_negative_predictive_values}{precision}.
\subsection{Other names}{

Positive Predictive Value
}
}
\examples{
## Generate actual
## and predicted classes
actual_classes    <- factor(c(1,2,1,2,2,3,4))
predicted_classes <- factor(c(1,2,1,2,2,3,4))

## Generate sample
## weights
sample_weights <- runif(
   n = length(actual_classes)
)

## Evaluate performance
SLmetrics::weighted.weighted.precision(
   actual_classes, 
   predicted_classes, 
   sample_weights
)
}
