% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S3_ReceiverOperatorCharacteristics.R
\name{auc.roc.curve}
\alias{auc.roc.curve}
\alias{weighted.auc.roc.curve}
\title{Area under the Receiver Operator Characteristics Curve}
\usage{
## Generic S3 method for
## unweighted area under the
## Receiver Operator Characteristics
## Curve
auc.roc.curve(...)

## Generic S3 method for
## weighted area under the
## Receiver Operator Characteristics
## Curve
weighted.auc.roc.curve(...)
}
\arguments{
\item{...}{
  Arguments passed on to \code{\link[=auc.roc.curve.factor]{auc.roc.curve.factor}}, \code{\link[=weighted.auc.roc.curve.factor]{weighted.auc.roc.curve.factor}}
  \describe{
    \item{\code{actual}}{A vector of vector}
    \item{\code{response}}{A matrix}
    \item{\code{method}}{A method}
    \item{\code{micro}}{What?}
    \item{\code{w}}{A <\link{numeric}> vector of sample weights.}
  }}
}
\description{
A S3 generic function for calculating the area under the receiver operator characteristics curve score of a classification model. \code{\link[=auc.roc.curve]{auc.roc.curve()}} handles the input as is - and therefore there is not sanity checks.
If the data contains \link{NA}, or \code{length(x) != length(y)} you are left at the mercy of compiler.
\subsection{Constructing data.frames}{

Description for the main function

Use \link{auc.roc.curve} for calculating the area under the curve directly.
}

\subsection{Defensive measures}{

As everything is based on pointers internally values as \link{NA} and out of bounds values (\code{length(x) != length(y)}) the compiler does not know how to react - this is undefined behaviour. And therefore it is not enough to wrap your call in \code{try()} or \code{tryCatch()} to recover from sudden errors. Your \code{R}-session \emph{will} most likely just crash.
A workaroud is to create a wrapper around \code{\link[=auc.roc.curve]{auc.roc.curve()}} and any other evaluation metrics you are planning to use, and do the sanity checks before it reaches \code{\link[=auc.roc.curve]{auc.roc.curve()}}.
}
}
\concept{Machine learning performance evaluation}
\keyword{classification}
\keyword{evaluation}
