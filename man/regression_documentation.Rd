% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SLmetrics-package.R
\name{regression_documentation}
\alias{regression_documentation}
\title{Regression Documenatation}
\arguments{
\item{actual, predicted}{A pair of <\link{double}> vectors of \link{length} \eqn{n}.}

\item{w}{A <\link{double}> vector of sample weights.}

\item{...}{Arguments passed into other methods}

\item{delta}{A <\link{double}>-vector of \link{length} \eqn{1} (default: \eqn{1}). The threshold value for switch between functions (see calculation).}

\item{correction}{A <\link{logical}> vector of \link{length} \eqn{1} (default: \link{FALSE}). If \link{TRUE} the variance and covariance
will be adjusted with \eqn{\frac{1-n}{n}}}

\item{k}{A <\link{double}>-vector of \link{length} 1 (default: 0). For adjusted \eqn{R^2} set \eqn{k = \kappa - 1}, where \eqn{\kappa} is the number of parameters.}

\item{normalization}{A <\link{double}>-value of \link{length} \eqn{1} (default: \eqn{1}). \eqn{0}: \link{mean}-normalization, \eqn{1}: \link{range}-normalization, \eqn{2}: \link{IQR}-normalization.}

\item{alpha}{A <\link{double}>-value of \link{length} \eqn{1} (default: \eqn{0.5}). The slope of the pinball loss function.}

\item{deviance}{A <\link{logical}>-value of \link{length} 1 (default: \link{FALSE}). If \link{TRUE} the function returns the \eqn{D^2} loss.}

\item{power}{A <\link{double}> value, default = 2.
Tweedie power parameter. Either power <= 0 or power >= 1.

The higher \eqn{power}, the less weight is given to extreme deviations between actual and predicted values.
\itemize{
\item \strong{power < 0:} Extreme stable distribution. Requires: predicted > 0.
\item \strong{power = 0:} Normal distribution, output corresponds to \code{\link[=mse]{mse()}}, actual and predicted can be any real numbers.
\item \strong{power = 1:} Poisson distribution (\code{\link[=deviance.poisson]{deviance.poisson()}}). Requires: actual >= 0 and predicted > 0.
\item \strong{1 < power < 2:} Compound Poisson distribution. Requires: actual >= 0 and predicted > 0.
\item \strong{power = 2:} Gamma distribution (\code{\link[=deviance.gamma]{deviance.gamma()}}). Requires: actual > 0 and predicted > 0.
\item \strong{power = 3:} Inverse Gaussian distribution. Requires: actual > 0 and predicted > 0.
\item \strong{otherwise:} Positive stable distribution. Requires: actual > 0 and predicted > 0.
}}
}
\description{
This roxygen block is the generic documnentation
for entropy metrics for all the parameters.
}
\keyword{internal}
