% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{baccuracy.factor}
\alias{baccuracy.factor}
\title{Baccuracy}
\usage{
\method{baccuracy}{factor}(actual, predicted, adjust = FALSE, na.rm = TRUE, ...)
}
\arguments{
\item{actual, predicted}{A pair of vectors <\link{factor}> with actual and observed classes.}

\item{adjust}{A \link{logical} value (default: \link{FALSE}). If \link{TRUE} the metric is adjusted for random chance \eqn{\frac{1}{k}}.}

\item{na.rm}{A <\link{logical}> value of \link{length} \eqn{1} (default: \link{TRUE}). If \link{TRUE}, \link{NA} values are removed from the computation.
This argument is only relevant when \code{micro != NULL}.
When \code{na.rm = TRUE}, the computation corresponds to \code{sum(c(1, 2, NA), na.rm = TRUE) / length(na.omit(c(1, 2, NA)))}.
When \code{na.rm = FALSE}, the computation corresponds to \code{sum(c(1, 2, NA), na.rm = TRUE) / length(c(1, 2, NA))}.}

\item{...}{Arguments passed into other methods.}
}
\value{
A <\link{double}>-value

A \link{numeric}-vector of \link{length} 1
}
\description{
A S3 generic function for calculating the baccuracy score of a Classification model. The \code{\link[=baccuracy]{baccuracy()}} handles the input as is - and therefore there is not sanity checks.
If the data contains \link{NA}, or \code{lenght(x) != lenght(y)} you are left at the mercy of compiler.
}
\examples{
## Generate actual
## and predicted classes
actual_classes    <- factor(c(1,2,1,2,2,3,4))
predicted_classes <- factor(c(1,2,1,2,2,3,4))

## Evaluate performance
SLmetrics::baccuracy(
   actual_classes, 
   predicted_classes
)
}
